{
  "name": "herhaven-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
